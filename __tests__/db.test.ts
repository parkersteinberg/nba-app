import { describe, expect, it } from '@jest/globals'
import { queryDatabase } from '@/database'
import { fetchInjuryData } from '@/database/fetchInjuryData'

describe('Testing db queries', () => {
  describe('queryDatabase tests', () => {
    it('Returns expected columns', () => {
      // make db call
      // check properties on results
      // expect(results).toHaveShape
    })
  })
})
